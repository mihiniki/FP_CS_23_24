#lang racket

#|
Define a recursive
and an iterative procedure for calculating "x" to the power of "n".
|#

(define (pow-rec x n)

)

(define (pow-iter x n)

)

(= (pow-rec 2 5) 32)
(= (pow-rec 15 3) 3375)

(= (pow-iter 2 5) 32)
(= (pow-iter 15 3) 3375)
